{
  "name": "open-timer",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "私密、准确、可自托管的工作计时器 · A private-by-default, self-hosted work timer",
  "author": "Evan-Joseph",
  "keywords": [
    "timer",
    "time-tracker",
    "productivity",
    "self-hosted",
    "sqlite",
    "docker",
    "cloudflare-workers"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Evan-Joseph/open-timer.git"
  },
  "bugs": {
    "url": "https://github.com/Evan-Joseph/open-timer/issues"
  },
  "homepage": "https://github.com/Evan-Joseph/open-timer#readme",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "scripts": {
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w web",
    "test": "npm run test -w shared && npm run test -w server",
    "test:e2e": "playwright test",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w server && npm run typecheck -w web"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2"
  }
}
