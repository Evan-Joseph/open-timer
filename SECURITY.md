# Security policy

Please do not open public issues for suspected vulnerabilities. Report them
privately to the repository maintainer with reproduction steps, impact, and
affected version. Never include API keys, database exports, session cookies,
or personal timer data in a report.

This project defaults to owner-only data access. Keep deployment secrets in
your host's secret store and rotate an AI provider key if it may have leaked.
