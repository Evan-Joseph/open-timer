# 11408 沉浸时钟 · 接手提示词（下一位接手者）

更新时间：2026-08-20（Asia/Shanghai）
适用工程：`/Users/joseph/Desktop/【考研】11408考研备考/【工具】11408沉浸时钟`

## 第一条提示词（直接复制给下一位接手者）

```text
你接手的是「11408 沉浸时钟」独立工程，不是旧的学习工作台，也不是 study-ledger。
先现场读取 docs/交接手册-沉浸时钟-2026-08-20.md（历史事实与 P0 契约）、docs/DESIGN.md（设计系统单一事实来源）、README.md、docs/API.md，再核对当前 Git 状态、package.json、wrangler.jsonc。
然后跑完整门禁：npm test、npm run typecheck、npm run build、npx playwright test --project=desktop-light、git diff --check、npx wrangler deploy --dry-run。不要凭历史摘要宣称完成，不要先改代码。

请先做一轮证据化审计：
1. 核对 shared/ 的状态机、休息策略（专注 1/5、钳制 2–20 分钟、75%/100%/150%+2min 宽限）、北京时间日切与汇总口径；核对 server/ 的 Hono 路由、鉴权、CSRF、限流（CF-Connecting-IP 优先）、幂等（全部会话写操作，含 note/retime/adjust-start）、SQLite/D1 双 adapter、静态资源 fallback 与统一安全头（server/src/headers.ts）；核对 web/ 的三态时钟、首页、近 7 天泳道、时间轴悬停/详情/编辑、设置、声音、动画。
2. 设计系统以 docs/DESIGN.md 为准：所有样式引用 styles.css 语义 token（颜色/圆角/间距/字级/动效/材质），禁止组件另写近似值；按钮层级、分段控件、弹层遮罩、三态主题（专注/休息 L1-L3/逾期/静默）都有明确定义。
3. UI/UX 只覆盖 Pad/Desktop 横屏（1024×640/768、1180×820、1280×720、1440×900），共用一套布局；全屏与窗口模式共用同一套代码，进入全屏只是视口变大，无第二套全屏 UI。不要重新引入旧学习工作台、日报计划、移动端 project、.workers.dev。
4. 前后端变更必须先写回归测试或可重复的几何/API 断言；完成后重跑完整门禁。部署前 npx wrangler deploy --dry-run，真实部署只使用 clock.4c666.top（需用户明确授权），部署后代理验证 health、daily-summary、sessions、state 与静态资源指纹一致性。

最终回报必须包含：发现的问题（按严重性）、修改文件、测试结果、截图或 API 证据、部署版本/域名、未完成项和剩余风险。不要修改四科仓库、study-ledger 或根工作区学习计划。
```

## 当前事实（接手基准）

- 分支 `codex/timeline-motion`；HEAD 见 `git log`（最近提交 `1d990d3` 全屏布局统一、`be2eccb` 契约与 token 统一）。
- 公网唯一 Base URL：`https://clock.4c666.top`；Worker `immersive-clock-11408`；D1 绑定 `DB=clock-11408`；静态资源绑定 `ASSETS`（`run_worker_first`）；`workers_dev: false`。
- 最新线上版本：`86a83a86-bb04-4764-ac29-d74a5c93697a`（对应 `1d990d3`），部署消息已携带 commit 号。
- 上一轮完整门禁：shared 46/46、server 20/20、desktop E2E 32/32、typecheck、build、dry-run 通过。

## 设计系统与交互规范

- 设计系统总纲：`docs/DESIGN.md`（token 表、按钮层级、弹层遮罩、三态主题、响应式骨架、参考项目取舍）。
- 历史审计：`docs/视觉审计-2026-08-11.md`、`docs/UI响应式与全局告警设计-2026-08-17.md`。
- 参考项目（只作学习，不作复制清单）：Super Productivity（21k★）、Pomotroid、FocusTide、Tomato、Apple HIG、Material Design 3、Refactoring UI。

## 待办（本轮已完成项见交接手册「接手完成记录」）

1. 视觉打磨：统一深色模式对比度、字体子集化（Noto Sans SC 约 2MB）、截图矩阵补深色/reduced-motion。
   （2026-08-20 已完成：深色 `--text-3` 提亮修复 surface-2 2.75:1 违规；style-review 加对比度契约断言；截图矩阵补深色四态 + reduced-motion。）
2. 备份定时任务（Cron Triggers + R2）：已实施（2026-08-20）。北京 23:00 写入 `clock-11408-backup`，滚动保留 30 天；events.jsonl 与导出端点共用 `eventToLine`。
3. D1 `pause/stop` 的 active_seconds 更新在 batch 之外（事件链仍是权威，可接受）。

## 发布门禁

```bash
npm test
npm run typecheck
npm run build
npx playwright test --project=desktop-light
git diff --check
npx wrangler deploy --dry-run
npx wrangler whoami
```

真实部署后代理验证（`--proxy http://127.0.0.1:7897`）：`/api/v1/health`、`/api/v1/daily-summary?date=YYYY-MM-DD&timezone=Asia%2FShanghai`、`/api/v1/sessions?date=YYYY-MM-DD`、`/api/v1/state`；并核验 `/` 的安全头与静态资源指纹。
