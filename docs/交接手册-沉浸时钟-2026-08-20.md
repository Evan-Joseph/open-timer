# 沉浸时钟接手手册

更新时间：2026-08-20（Asia/Shanghai）  
适用工程：`/Users/joseph/Desktop/【考研】11408考研备考/【工具】11408沉浸时钟`

## 给下一位接手者的第一条提示词

```text
你接手的是“11408 沉浸时钟”独立工程，不是旧的学习工作台，也不是 study-ledger。
先现场读取本文件、README.md、docs/API.md、docs/design-phase1.md、当前 Git 状态、package.json、wrangler.jsonc，再运行 npm test、npm run typecheck、npm run build 和 npx playwright test --project=desktop-light。不要凭历史摘要宣称完成，不要先改代码。

请先做一轮证据化审计：
1. 核对 shared/ 的状态机、休息策略、北京时间日切与汇总口径；核对 server/ 的 Hono 路由、鉴权、幂等、限流、SQLite/D1 adapter、静态资源 fallback；核对 web/ 的三态时钟、首页、近 7 天泳道、时间轴悬停编辑、设置、声音、动画与全屏检测。
2. 只看 Pad 横屏和 Desktop 横屏，共用一套前端；不要重新引入旧学习工作台、日报计划或学习掌握判断。
3. UI/UX 先参考并记录对照证据，再改代码。优先看 Apple Human Interface Guidelines、Super Productivity、Pomotroid、Motion 文档和本工程 docs/design-phase1.md；低 Star 仿作只能作为灵感，不能作为规范来源。
4. 当前明确待办：左上角名称替换或隐藏；统一颜色、字体、圆角、透明度、按钮、间距、弹层与文案；在设置内恢复“进入全屏”入口，同时保留外部全屏自动识别；优化 Pad/Desktop 的首屏布局、休息提示、时间轴详情与近 7 天视图。
5. 前后端变更必须先写回归测试或可重复的几何/API断言；完成后重新跑完整门禁。部署前用 npx wrangler deploy --dry-run，真实部署只使用 clock.4c666.top，部署后代理下验证 health、daily-summary、sessions、state。

最终回报必须包含：发现的问题（按严重性）、修改文件、测试结果、截图或 API 证据、部署版本/域名、未完成项和剩余风险。不要修改四科仓库、study-ledger 或根工作区学习计划。
```

## 当前事实

- 当前 Git 分支：`codex/timeline-motion`。
- 当前 HEAD：`ed1d764 fix: stabilize dense timeline layouts`。
- 公网唯一 Base URL：`https://clock.4c666.top`。
- Wrangler 配置：Worker `immersive-clock-11408`，custom domain `clock.4c666.top`，D1 绑定 `DB=clock-11408`，静态资源绑定 `ASSETS`。
- 最近一次已记录的线上发布版本：`d0c60f34-1386-407a-bd48-ad15fe2802b5`。接手后仍需现场用 `wrangler deployments list` 或线上 health 刷新版本事实。
- 最近一次完整门禁记录：shared 46/46、server 12/12、desktop E2E 33/33、typecheck、build、Wrangler dry-run 通过。此记录属于上一轮发布证据，不能替代本轮验证。

线上 API 只读路径：

```text
GET /api/v1/health
GET /api/v1/subjects
GET /api/v1/state
GET /api/v1/sessions?date=YYYY-MM-DD
GET /api/v1/daily-summary?date=YYYY-MM-DD&timezone=Asia%2FShanghai
```

写请求均需 owner cookie。创建、暂停、继续、结束、换科、作废已使用 `Idempotency-Key`；备注、retime、adjust-start 与文档宣称的“所有写请求均幂等”存在契约漂移，接手后应先审计并决定统一实现或修正文档。公网只读数据是执行事实，不是学习完成、正确率、掌握或复习证据；这些边界由 `study-ledger` 独占。

## 代码边界

```text
shared/   领域规则：状态机、净时长、北京时间日切、日报汇总、静默时段、休息预算
server/   Hono API、鉴权、CSRF、限流、幂等、SQLite/D1 存储、Worker 入口
web/      React/Vite 页面、时钟三态、时间轴、7 天回顾、设置、声音、动画、全屏布局
migrations/  SQLite/D1 交集 SQL
e2e/      Playwright 核心流程、响应式、视觉样式和数据状态回归
docs/     API、设计方案、视觉审计、响应式与交接材料
```

禁止事项：恢复“学习工作台”路线；在时钟中写学习事件；从计时推断学习质量；绕过 API 直连数据库；把历史摘要或某次测试输出当成当前线上事实。

## 已实现的关键行为

1. 服务端时间是事实来源，前端用单调时钟平滑显示并重新校准。
2. 同一用户最多一个活动会话；暂停/继续段保持同一时间轴泳道。
3. 休息预算基于上一段专注时长：约为专注时长的 1/5，最少 2 分钟、最多 20 分钟；提醒阶段为 75%、100%、150%（具体阈值以 `shared/src/rest-policy.ts` 为准）。
4. 专注运行中不触发离开提醒；暂停和科目结束后才进入休息状态；午饭、午睡、晚饭、夜间等静默窗口不升级提醒。
5. 近 7 天只展示已结束段，固定 08:00–22:30 全天泳道；打开 7 天视图时隐藏单日时间轴。
6. 7 天视图和全屏时间轴已针对真实累积数据做过固定视口回归；全屏抽屉打开后使用真实网格行，不依赖固定负位移。
7. 时间轴片段支持悬停预览、点击详情、备注编辑、起点补录、撤回/作废；事件链保留审计记录。
8. 全屏当前由浏览器状态自动识别；现状已有外部进入全屏后的布局切换，但应用内“进入全屏”入口尚未按本次要求恢复。

## 当前审计重点

### P0：先保护的契约

- 任何 UI 改动不得改变 `subject_id`、历史 session、统计和 API 字段兼容性。
- 不得让 `ended_at: null` 的 running/paused 开放段出现在近 7 天泳道。
- 不得在 running 状态显示休息提醒；paused/结束反馈才允许显示休息预算和逾期阶段。
- 不得让撤回、起点补录、暂停/继续后的时间轴和日报出现不同步。
- 不得重新引入 `.workers.dev` 作为产品 Base URL；公开文档统一使用 `clock.4c666.top`。
- 核对写请求幂等契约：`PATCH /sessions/:id/note`、`POST /retime`、`POST /adjust-start` 当前未统一经过 `withIdempotency`，但 README/API 文档写成“所有写请求必须携带 Idempotency-Key”。先用测试固定预期，再改实现或文档。
- 修正部署文档漂移：README 的迁移章节仍写“前端静态走 Pages”，当前生产配置实际是 Worker Static Assets，`ASSETS` 绑定并启用 `run_worker_first`。

### P1：本轮产品打磨

- 左上角“沉浸时钟”名称：评估隐藏、短品牌名或纯图标方案；需同时检查页面标题、ARIA、favicon/分享标题和测试选择器。
- 设置内恢复“进入全屏”按钮：使用熟悉的全屏图标，浏览器拒绝或权限失败时给出可理解反馈；外部 F11/DOM Fullscreen 仍自动识别。
- 建立单一视觉 token 表：背景、表面、边框、主色、危险色、文字层级、圆角、阴影、材质透明度、动效时长。禁止组件各自写一套近似值。
- 统一按钮层级：主操作、次操作、危险操作、图标操作、分段选择器、时间轴控制器分别定义尺寸和焦点态；图标按钮提供 tooltip/ARIA，不用文字圆角胶囊替代熟悉图标。
- 统一弹层透明度和层级：设置、时间轴详情、全屏抽屉不能互相套卡；检查 backdrop-filter 退化、深色模式对比度和滚动锁定。
- Pad/Desktop 横屏共用一套布局：至少验证 1024×768、1180×820、1280×720、1440×900；首屏不产生文档滚动，不遮挡时钟、休息提示、时间轴和当前时间 Flag。
- `playwright.config.ts` 仍保留 Pixel 7 mobile project；本轮产品要求不再投入移动端，接手者应确认是否退役该 project，Pad/Desktop 验收不能继续由手机项目代替。
- 文案精简：删除重复免责声明、无意义百分比、无法解释的同比指标；只保留能帮助当前决策的时长、状态、范围和动作。
- 动效统一：优先 CSS transition + `motion/react` 现有能力；每个状态切换定义进入、退出、布局位移和 reduced-motion 行为，不新增 GSAP 作为默认依赖。

### P2：前后端质量审计

- 检查公开只读 API 的数据最小化、缓存/ETag、CORS、错误响应和速率限制；确认备注等字段是否仍符合公开读取的产品决策。
- 检查 D1 与 SQLite adapter 行为一致性：事务、唯一活动会话、事件链、幂等响应清理、UTC 存储和 Asia/Shanghai 汇总。
- 检查静态资源 fallback：`run_worker_first`、API 404 与 SPA fallback 不得互相吞错；线上必须分别验证 API 和 `/`。
- 检查前端轮询、可见性恢复、多标签页 BroadcastChannel/localStorage 同步、后台节流后的服务端时间校准。
- 检查声音：浏览器 autoplay 解锁、默认音量、结束提示音和环境音启停；不得把系统/耳机的绝对响度写成应用可保证的事实。

## 参考资料与使用方式

这些是学习入口，不是直接复制清单。接手者应记录“参考行为 → 本项目差异 → 保留/舍弃理由”。

- Apple Human Interface Guidelines：<https://developer.apple.com/design/human-interface-guidelines/>。用于层级、触控目标、材质、可访问性、全屏和系统行为边界。
- Apple Layout：<https://developer.apple.com/design/human-interface-guidelines/layout>。用于 Pad/Desktop 横屏的安全区、留白、分栏和稳定布局。
- Super Productivity：<https://github.com/super-productivity/super-productivity>。约 2.1 万 Star、持续更新；学习时间跟踪信息架构、密度、状态和桌面工作流，不复制其任务系统。
- Pomotroid：<https://github.com/Splode/pomotroid>。学习番茄计时器的阶段切换、桌面聚焦和声音反馈；实现前核对仓库当前状态。
- Motion for React：<https://motion.dev/docs/react>。学习可访问的状态过渡、布局动画和 reduced-motion；保持现有 `motion/react` 依赖，除非有可验证收益。
- GSAP：<https://gsap.com/docs/v3/>。只作为复杂编排的候选参考；本项目当前动效量不证明新增依赖必要性，先用 CSS/Motion。
- Cloudflare Workers Static Assets：<https://developers.cloudflare.com/workers/static-assets/>。用于检查 API 优先级、SPA fallback、缓存和部署验证。

本次 GitHub 检索结果中大量低 Star“Apple 仿作”和未维护模板不纳入规范来源。优先读原仓库源码、文档、Issue 和最近提交，再决定是否借鉴。

## 推荐实施顺序

1. 复现当前门禁并刷新线上事实；建立问题清单和截图矩阵。
2. 先写视觉/交互规格：品牌名、信息层级、token、按钮和弹层状态、Pad/Desktop 断点、全屏入口。
3. 先改共享 token 和骨架，再改单个组件；每次变更同步更新 style-review 和响应式断言。
4. 处理首页休息状态、7 天回顾、时间轴详情/编辑和全屏抽屉；确保状态机和 API 不被 UI 旁路。
5. 做前后端契约审计与性能检查，再进行发布 dry-run。
6. 真实发布前要求用户明确授权；只发布 `clock.4c666.top`，不操作 DNS、付费资源或其他项目。

## 发布门禁

```bash
npm test
npm run typecheck
npm run build
npx playwright test --project=desktop-light
git diff --check
npx wrangler deploy --dry-run
npx wrangler whoami
```

真实部署后，使用项目规定代理重试验证：

```bash
curl --proxy http://127.0.0.1:7897 --retry 4 --retry-all-errors \
  https://clock.4c666.top/api/v1/health
curl --proxy http://127.0.0.1:7897 --retry 4 --retry-all-errors \
  'https://clock.4c666.top/api/v1/daily-summary?date=YYYY-MM-DD&timezone=Asia%2FShanghai'
curl --proxy http://127.0.0.1:7897 --retry 4 --retry-all-errors \
  'https://clock.4c666.top/api/v1/sessions?date=YYYY-MM-DD'
curl --proxy http://127.0.0.1:7897 --retry 4 --retry-all-errors \
  https://clock.4c666.top/api/v1/state
```

代理第一次 TLS 失败不能记成 API 404；必须保留 HTTP 状态、响应摘要和重试结果。

## 交接完成标准

- 新接手者能在 10 分钟内知道代码边界、线上域名、当前 HEAD 和不可破坏契约。
- UI 方案有参考来源、对照截图和明确的取舍记录，不以“苹果风”作为无证据主观结论。
- Pad/Desktop 横屏关键状态有自动化几何断言和浅色/深色/reduced-motion 截图。
- API、D1、静态资源 fallback、时间轴和休息状态有独立验证证据。
- 交付报告区分已完成、待验证、阻断和范围外事项；不把视觉草图或计划写成完成状态。

## 2026-08-20 接手完成记录（补记）

本轮接手者（qwen3.8-max / DSH）完成事项，全部有测试与门禁证据：

1. 幂等契约统一：`PATCH note`、`POST retime`、`POST adjust-start` 接入 `withIdempotency`；回放保持原状态码（v2 存储信封 `{"v":2,"status","body"}`，兼容 v1 裸 body 记录）；过期键清理移入 `withIdempotency`，不再依赖 start 单一入口。测试：`server/src/app.test.ts` 幂等统一用例。
2. 静态资源安全头：`server/src/headers.ts` 单一实现，Worker 静态响应与 API 同源携带 CSP/XFO/nosniff/referrer/HSTS；防闪白脚本外置为 `web/public/theme-init.js`，CSP 收紧为 `script-src 'self'`（无 unsafe-inline）。
3. 限流 IP：优先 Cloudflare 边缘写入的 `CF-Connecting-IP`，`X-Forwarded-For` 仅作非边缘降级（`headers.ts` clientIp + 单测）。
4. API 404：`createApp` 内统一 `app.notFound` JSON `NOT_FOUND`；CORS 预检 `app.options('/api/v1/*')` 返回 204 并豁免 CSRF。
5. 文档对齐：README/API.md/openapi.yaml（补 start、switch、note、retime、adjust-start、auth、credentials、export 全部路径与幂等契约）、migration-checklist（Pages → Worker Static Assets 现状）。
6. mobile（Pixel 7）Playwright project 退役（用户决策）；Pad/Desktop 断点回归扩展至 1024×640/768、1180×820、1280×720、1440×900。
7. P1 产品：顶栏品牌名隐藏（document.title 与 `<title>` 保留识别职责）；设置内恢复“进入全屏”入口（拒绝时给出可理解反馈）；视觉 token 表补全（`--ease-standard`、时长、遮罩、向上阴影），按钮/弹层/抽屉过渡全部取 token。
8. P2 审计结论：双适配器 SQL 与审计结构一致（D1 `pause/stop` 的 active_seconds 更新在 batch 之外，事件链仍是权威，接受）；声音默认关、45% 音量、手势解锁、响度免责文案齐备；公开 API 备注字段为既有产品决策（API.md 已声明），未改动。
9. 全屏布局统一（用户 2026-08-20 复核指令）：删除 `fullscreen-mode` 分支、`fs-controls` 控制条与 `timeline-drawer` 抽屉；全屏与窗口模式共用同一套组件和 CSS，进入全屏只是视口变大，尺寸由既有 dvh/clamp 响应式规则自适应。对应 E2E 改为断言“同一布局 + 无第二套全屏 UI”。
10. 交付前打磨（用户 2026-08-20 接手准备指令）：时间轴「默认/全天」尺度控件从居中悬浮移入 `.timeline-nav`，与标题、昨天切换、流水账视图按钮对齐；统一两套分段控件（`.seg-control` 与 `.timeline-scale` 共用灰底+白块选中配方）；补齐 `.icon-btn.selected` 激活态；新增间距/字级 token。设计系统单一事实来源沉淀为 `docs/DESIGN.md`，接手提示词沉淀为 `docs/接手提示词-沉浸时钟.md`。

剩余待办：线上版本对应提交无部署消息可证（下次部署用带 commit 信息的消息）；workers.dev 已按用户决策禁用（workers_dev: false），如总控需要备用读入口另行评估。

## 2026-08-21 误触保护与交互审计（补记）

用户把「继续」点成「结束」，暴露误触不可恢复问题。选定修法：**误触后可继续**（结束保持单击，不加长按/滑动摩擦）——撤销模式优于阻断模式，结束是高频操作。

**状态机契约变更**：`stopped → resumed` 放开（误触继续）。重开会话清除 `ended_at/end_reason`、保留原段与秒数、开新开放段；事件 payload 带 `reopened_from: 'stopped'`，审计 `session_resume` 带同款标记。守卫：已有其他活动会话时重开返回 409 `ACTIVE_SESSION_EXISTS`（唯一活动会话约束）。shared/server/前端/文档/测试全部对齐。

**入口**：结束反馈卡「▶ 继续这段」（主操作，居右）；时间轴详情弹窗「继续这段」（2×2 动作网格，撤回右下）。覆盖「点错当时」与「关掉反馈卡后才意识到」两个场景。

**附带修复**：离开锚点改为单调不回跳（休息秒数只增不减；轮询校准竞态不再让「已休息」数字倒退）——3s 轮询下假时钟 E2E 暴露，真实场景同样受益。

**全量交互审计结论**（44 处可点击元素逐一过）：
- ✅ 已保护：结束（可继续）、暂停/继续（可逆）、换科（在折叠 details 内，时间不丢失）、备注/起点编辑（可再改、有审计）。
- ⚠️ 待办（需迁移，单独处理）：**撤回（void）不可撤销**。voided 是终态，恢复需 `voided → stopped`，而 `session_event.kind`/`manual_adjustment.kind` 的 CHECK 约束需重建表迁移（且 D1 目前只硬编码加载 0001，SQLite 读整个 migrations 目录——迁移加载方式本身需先统一）。不做半套实现以免破坏「事件链可重放」正确性。
- ✅ 低风险不改：登出（单用户重登即可）、导航/视图切换（全部可逆）。

测试：shared 48/48、server 30/30（+重开流程/守卫/回放）、desktop E2E 39/39（+误触继续两路径）。

## 2026-08-21 同步与计时正确性审计（补记）

本轮围绕「多端同步 + 计时/北京时间对齐」做了联网调研（MDN BroadcastChannel/storage/SharedWorker、Cloudflare D1 batch 语义、NTP offset 公式、HLC、IETF idempotency-key draft、Cloudflare Workers Performance 冻结语义）与代码审计，修复如下：

**正确性 bug（P0/P1）**
1. `daily-summary` 今日/含 running 会话仍返回基于 revision 的 304：running 秒数在涨但 revision 不变 → 带 If-None-Match 拿到错误 304。修复：running 期间禁用 304、不设 ETag（`app.ts`）。
2. `retime` 对汇总完全无效：只改 `active_seconds` 快照，而 state/daily-summary/sessions 都按段重算。修复：retime 落到末段结束时刻，汇总按段重算即反映；使末段为负时 400 `INVALID_RETIME`（两 adapter）。
3. `revision`（ETag 源）只覆盖 `session_event`，note/retime/adjust-start 不写事件 → ETag 静默失效。修复：`maxEventId` 改为审计日志最大 id（audit_log 覆盖所有写操作，含 note/retime/adjust-start）。
4. D1 `pause/stop` 的 active_seconds 更新在 batch 外（事务外窗口，与 SQLite 不一致）。修复：并入 batch，用确定性 SQL 子查询，两 adapter 字节一致；删除 D1 死代码 `computeActiveSeconds`。
5. 取整口径不一致：`summary` 每段分别 floor vs `computeActiveSeconds` 总 ms 一次 floor（多段会话差 1 秒）。修复：summary 按 session 累加 ms 后一次 floor。

**前端同步（跨标签/跨设备/时钟校准）**
6. 同源多标签：BroadcastChannel 主通道 + storage 事件兜底（替换 localStorage 写盘脉冲），收到脉冲立即 refresh 服务端校正。
7. bfcache/冻结恢复：补 `pageshow(persisted)` + `resume` 监听（此前只监听 visibilitychange/focus）。
8. RTT 半程锚点：refresh 记录发出时刻，锚点配到「请求-响应中点」，消除系统性 RTT/2 偏差。
9. 运行中轮询 5s→3s（跨设备收敛延迟 <3s）；sessions 日期改走服务端 `today_date`（不再依赖客户端墙钟，跨午夜正确）。
10. 文档漂移：README/migration-checklist 密码哈希「argon2id」→ 实际 PBKDF2-SHA256。

**时区防御**
11. `isValidShanghaiDate` 拒绝 1992 前日期（中国夏令时 1986–1991，1991-09-15 结束）；shanghai.test 加「1992 起 +8 无 DST 运行时断言」——该断言实测暴露了 1991 年夏季仍 +9，据此把阈值从 1991 修正为 1992。

**明确不做**（避免过度工程，调研依据见 DESIGN.md 参考资料）：不引入 WebSocket/DO/SSE（单用户轮询最优，3s 收敛）；不引入 HLC（单写者 + 事件自增 id 已全序）；不做钟漂率补偿（3s 重校准 + 1.2s 阈值足够）；不引入 date-fns-tz/Temporal（固定 +8 对 1992 后精确）；active_seconds 快照加速不必要（事件量极小，重放零成本）。

测试：shared 48/48、server 29/29（新增 ETag running、retime 落段、note revision 前进、审计语义）、desktop E2E 37/37。

## 2026-08-21 空态误报 / 召回弹窗移除 / 设计语言收敛（补记）

用户报「这一天还没有记录」在错误时机出现、逾期单独弹窗多余、以及全局设计语言需再统一。

**问题 1 · 空态误报（两个根因）**
- 主因：轨道空态用 `visibleSegs.length === 0` 判断。默认尺度是锚定当前时刻的 4h 窗口，当天有记录但都在窗口外（如上午学过、现在下午）时误报「这一天还没有记录」，而下方科目小计仍显示时长，自相矛盾。改为按 `segs.length === 0`（整天维度）判断；「有记录但不在当前窗口」单独给弱化提示 `.timeline-empty-window`。
- 次因：切换到未缓存历史日时 `historySessions` 在 fetch 返回前仍是上一天旧数据，加载窗口期闪现错误内容/空态。新增 `dayLoading`，加载时清空旧数据并显示「正在读取…」。
- 回归测试：mock「记录在清晨 / state 在晚间」复现窗口外语义 + 真空日对照。

**问题 2 · 移除阻断式全屏召回弹窗**
- L3 逾期不再弹 `.away-overlay`（含「再等 5 分钟 / 回到学习 / 开始下一段」）：红色洗色氛围 + away-line 文案已足够表达，恢复入口在常规控件（继续计时 / 空闲页开始）。删除相关 state（awaySnoozedUntil/awayDismissed）、handler、JSX 与 CSS（`.away-overlay*`、`overlay-in`）。
- 两处依赖弹窗的 E2E 改写为断言红色洗色（data-away-level=3）+ away-line「休息已超时」+ 常规控件恢复。

**问题 3 · 设计语言收敛**
- 动效：`dialog-in 0.25s cubic-bezier(...)` → `var(--dur-enter) var(--ease-standard)`（消除最后一处内联缓动）。
- 颜色：新增 `--on-accent`（accent 实心底上的白），实心主按钮 `color: #fff` → token。
- 间距：`.seg-popover` padding 18px → `var(--space-4)`；`.text-btn` padding 0 10px → `0 var(--space-2)`。
- 保留有意为之：大数字字号（dvh/clamp 例外）、响应式密度微调 padding（受固定视口约束）、`.start-btn` 40px hero 内边距、分段控件 2px 微间距。

测试：desktop E2E 41/41（新增空态 2）；typecheck/build/diff-check/dry-run 全绿。

## 2026-08-21 近 7 天回顾改为居中浮层（补记）

用户反馈：看 7 天时下方留白、上方时钟被挤压。根因：旧实现用 `.app:has(.history-strip)` 把大时钟压到 `clamp(46px,7.8dvh,72px)`、主区钉死 40.4dvh，而周图内容（≈285px）撑不满分到的 1fr 行 → 既挤压主角又留白（单屏竖向空间零和）。

**修法**（联网调研 Pomotroid/FocusTide/Super Productivity/shadcn/Radix/Mantine/Apple HIG 后选定「居中 overlay 面板」范式——计时类应用普遍让时钟常驻主角、统计做定尺寸浮层）：
- `Timeline.tsx` 的 `history-strip` 从内联改为 `position: fixed` 居中浮层（`.history-overlay-backdrop` + `.history-overlay-panel`），fragment 包裹；加 Esc 关闭；移除原 auto-scroll 逻辑。
- 面板参数按调研：`min(960px, 100vw-64px)`、`max-height: 100dvh-64px` 内滚兜底、遮罩 `--overlay-scrim`、材质 `--popover-surface` + blur、250ms 入场、X/Esc/遮罩三通道关闭。
- 删除全部 8 条 `.app:has(.history-strip)` 压缩规则 → 时钟恢复全尺寸（1280×720 计时字号 56→79px、主区 295→624px），留白消失。
- E2E：关闭浮层改用 Esc（toggle 在遮罩后不可点）；`.history-strip` class 选择器改 `.history-overlay-panel`（testid 保留）。

测试：desktop E2E 41/41；门禁全绿。

## 2026-08-21 按钮权重 / 多端偏好同步 / 10s 误触过滤 / 空格主控（补记）

详见 `docs/DESIGN.md` §3 按钮权重规则与 §8 多端同步与误触。要点：
- 按钮权重：结束卡「好，继续」升 primary、「继续这段」降 ghost（<10s 误触时例外提权）；时间轴详情删备注 Save 按钮改 Enter/失焦自动保存；调研来源 HIG/SP/FocusTide/Pomotroid/Clockify。
- 多端偏好同步：`user_pref` 表（migration 0002）+ `GET/PUT /api/v1/prefs`；localStorage 即时层 + 10s 轮询 + 500ms 防抖推送 + 3s 在途窗口；音量不同步默认 0。
- 10s 误触过滤：`isCountedSegment` + `CLOCK_MIN_SEGMENT_SECONDS`（默认 10）。
- 空格键主控：Space=开始/暂停/继续/确认关闭。
- 提交：ffd4395。

## 2026-08-22 同步失效修复 + 日均口径 + 生产事故热修复（补记）

1. **同步失效根因**：`worker.ts` 曾以「`schema_migrations` 表存在」跳过迁移，生产库已有该表 → 0002（user_pref）永不执行，偏好读写静默失败。修复：移除门槛，每 isolate 首次请求执行幂等迁移（09a05b9）。
2. **生产事故（已恢复）**：09a05b9 部署后 D1 `exec(拼接 SQL)` 解析失败（多语句+注释混合，D1 已知缺陷）→ 全站 1101。热修复 685f048（线上 `6b8832b3`）：`splitMigrationSql` 按分号拆分（先剔注释）+ `batch` 原子执行，弃用 exec；新增单测。
3. **教训**：D1 上避免 `exec` 多语句 SQL；迁移类变更必须在全新本地 D1（`wrangler dev --persist-to` 新目录）冷启动验证后再上生产；生产验证不能只看匿名 401（不触达存储层），要覆盖登录态读写路径。
4. **日均口径**：近 7 天回顾「日均」只按过去 6 个完整日计算，进行中的今日不纳入分母（指标带 tooltip 注明）。
5. 测试：shared 54、server 37（+splitMigrationSql）、E2E 42（+跨标签页同步回归）；门禁全绿。

## 2026-08-22 对表核验 + 资源精算 + P0 索引修复（补记）

联网核验（NTP RFC 5905 / Cristian 1989 / chrony / 微软 Live Share SDK / timesync 源码 + Cloudflare 官方限额文档）结论：

**对表逻辑 = 教科书级 Cristian 算法（中点锚定），保留**：与微软 Live Share SDK 的 TimestampProvider 几乎逐行同构；1.2s 迟滞与 chrony `makestep 1.0` 同量级；秒级显示不需要 NTP 多采样（误差预算被 RTT/2 ≪ 1s 占据，3s 轮询本身是持续重采样）。**唯一漏洞已修**：重锚增加 RTT 样本防护——维护最近 8 次 RTT 窗口，样本 RTT > max(1000ms, 3×中位数) 时不用它重锚（防弱网锚点污染 ±RTT/2，Live Share improveAccuracy 思想）。不引入 Marzullo/HLC/钟漂率补偿（单权威源下均过度工程）。

**资源精算与 P0 修复**：
- **P0（已修）**：`sessionsOverlapping` 原无可用索引 → 全表扫描。D1 按行读计费（免费 500 万/天，超限当日全库报错），约 250 个历史会话即撞穿。修复：migration 0003 加 `session_ended(ended_at_ms)` 索引 + 查询改写为「索引 range ∪ 活动会话（部分索引 ≤1 行）」，行读从 O(全部历史) 降到 O(当日)。
- Workers 请求免费档 100,000/天是第二约束：空闲轮询 15s→30s、偏好轮询 10s→15s（已实施）。单设备 ≈2 万请求/天、三设备内安全。
- R2 备份 ≈4 次 Class A/天，永不是瓶颈。
- 明确不做：合并 /state+/sessions 端点（收益有限、改动大）、run_worker_first 调整（需安全头回归）、ETag（304 不省请求）。

**同步语义补全**：`historyOpen` 补消费端（此前只推不应用）；Esc/遮罩/X 关闭也同步关闭状态（统一 `closeHistory` 出口）；跨设备测试改用独立 browser context（同 context 共享 localStorage 会掩盖服务端同步路径）。

测试：shared 54、server 37、E2E 45（4 个跨设备同步用例：主题/选科/7天面板/计时对表±2s）；门禁全绿。

## 2026-08-23 神奇海螺：下一步推荐功能上线（补记）

新功能「神奇海螺」（代码标识 `conch`）：按各科目计时时间线 + 备注，由 LLM 推断学习节奏并给出「下一步最应该做什么」。完整设计（流程/字段/提示词/排版/缓存纪律/安全）见 `docs/神奇海螺-下一步推荐-设计-2026-08-23.md`。

**模型选型 = 实测 bake-off 结论**（非拍脑袋）：SiliconFlow `deepseek-ai/DeepSeek-V4-Pro`。同输入对比：仅它能正确检出「某章缺强化题」类缺步且措辞果断（JSON 合法）；`enable_thinking:false` 7s 但漏检缺步；V4-Flash 99s、GLM-5.2 超时，淘汰。默认思考 ~40s 由加载动效承接；嫌慢可设 `CONCH_THINKING_BUDGET`。

**关键契约**：
- `POST /api/v1/conch/ask`（owner-only、独立限流 20 次/时）：全量取会话（走 0003 索引口径）→ 只保留已 stopped 且通过误触过滤的专注事实 → 活动门槛（近 7 个北京日无已完成有效会话的科目不进提示词）→ 紧凑行编码（>150 行按 45 天切月度聚合）→ LLM → 字段级宽容解析（枚举收敛、未知科目丢弃、截断）→ 结构化响应。进行中/暂停信息仅前端实时展示，不进入决策输入。
- 密钥只在服务端（wrangler secrets：CONCH_API_BASE/CONCH_API_KEY/CONCH_MODEL），日志不落备注与 LLM 体。
- **缓存纪律（省 token 核心）**：`/state` 新增 `revision`（= maxEventId）；客户端按窗口分桶缓存，以 revision 校验——时间线无事件则反复打开零消耗；有事件即失效；兜底 30min TTL + 7 天上限。
- 无活跃科目不调 LLM 直接返回 skipped（not_started/inactive 两态）。

**前端**：时间轴工具栏海螺入口 + 居中浮层（复用近 7 天范式，720px）；窗口分段控件（从始至今/30 天/7 天）；推荐卡（科目色点、动作 chip、置信度、节奏/备选、一键「开始这个科目」以推荐语预填 intent_note）；`conchOpen` 并入多端偏好同步；左侧色条按设计审计意见移除（项目无此惯用法，色点已足够）。

测试：shared 66、server 41、E2E 44/45（唯一失败为改动前已存在的 style-review 移动端断言，已用 stash 对照证实非本次引入）；门禁全绿。2026-08-23 两次部署（Version 43a772c7 功能上线、595312c8 revision 缓存与打磨）。

## 2026-08-23 空闲态逾期氛围修复（补记）

**Bug（用户发现）**：两种空闲态休息逾期的视觉效果不一致——中途暂停逾期有红色全视口洗色，结束写备注后逾期只剩胶囊文字红。根因：空闲态渲染分支 `<section className="clockface idle">` 自 V1 起漏挂 `data-away-level`，而洗色由 `.app:has(.clockface[data-away-level])` 触发。非有意设计（设计 §5 三态本就要求统一氛围），已修：空闲态补挂该属性 + e2e 断言（关掉结束卡后 `.clockface.idle` 仍为 level 3，直到开始下一段）。

**顺带修**：结束态离开锚点原用本机 `Date.now()`，暂停态用服务端校准——设备时钟偏移会让两种空闲态休息计时快慢不同（逾期时刻不一致的第二来源），统一改为 `readServerNowMs()`。

## 2026-08-23 默认只读监督态（补记）

用户需求：让别人监督进度时无需交出密码——**默认进入只读态，想操作再解锁**。

- `AuthPhase` 增 `'readonly'`：`/auth/me` 已设置密码但未登录 → 只读态（替代原阻断式登录页）；首次未设置密码仍走 setup 阻断（必须先建密码）。`logout` 回到只读态而非登录页。
- 只读态封死：开始/暂停/继续/结束/换科目按钮与空格主控、时间轴备注编辑/更新起点/继续这段/撤回、神奇海螺（服务端要调 LLM，不给未登录者花 token）、设置里的退出登录按钮。顶栏加「只读监督」徽标 + 锁图标解锁层（复用 PIN 卡，遮罩叠加不阻断背后时钟，Esc/遮罩可关）。
- 空闲态只读文案：隐藏选科/目标输入/开始按钮，改显「今天累计 / 最近：科目·备注 / 只读监督模式提示」。运行/暂停态照常显示计时与休息提醒（监督者要看的就是这些）。
- 安全边界：写端点服务端本就 `requireOwner`，只读态是 UI 对齐而非鉴权替代；偏好轮询/推送仍仅 owner（401 静默）。
- e2e：三个 spec 的 setup helper 均适配「只读态 → 点锁解锁」路径；跨设备同步用例保持"另一端登录后跟随"语义。

## 2026-08-24 同步下线 + 设备角色（副屏/主控）+ 审计（补记）

**1. 浮层开合态同步下线**：`historyOpen`/`conchOpen` 从 `SyncedPrefs` 移除，改为纯设备本地（localStorage 持久化、刷新恢复）。动因：开合同步会让多端各自打开浮层重复发请求——海螺是昂贵 LLM 调用。主题/动画/音效/时间轴尺度等纯偏好同步保留。`e2e/flow.spec.ts` 原「7 天面板跨设备跟随」用例改为反向断言（不跟随）。

**2. 设备角色**（`web/src/lib/device.ts`）：`detectDeviceRole()` = 手动覆盖（`?role=`/localStorage `clock-device-role`）→ UA（iPad 显式、iPadOS 伪装 Mac 触控点>1、Android 非 Mobile）。**副屏（Pad）= 显示位**：隐藏海螺与近 7 天浮层入口（连同渲染兜底），保留时钟、计时控制、时间轴与显示类设置；与 2026-08-23 的「默认只读监督态」天然组合：Pad 未登录即纯监督屏。主控（电脑）全功能。

**3. 审计结论**（本地桩 LLM + Playwright 截图核验：主控空闲/海螺浮层/7天浮层/Pad 只读/Pad 登录）：海螺浮层排版、跳过科目弱态、卡片层级、副屏工具栏均正常；上一轮的动画名误用（`breathe-pill`→`pill-breathe`）与加载提示已修。**遗留观察**：一次合成数据（adjust-start 造出跨天巨长会话）截图中大时钟短暂显示异常值，两次干净复现均与服务器时间精确一致、无法再现——若真实使用再现，从 store anchor 链路查起。

门禁：shared 66 + server 41 全过，typecheck 0 错，E2E 44/45（唯一失败仍是已证实的前置 style-review 断言）。

## 2026-08-24 海螺开工 → 结束备注预填（补记）

按海螺推荐「开始这个科目」创建的会话，结束反馈卡弹出时把推荐语**预填为结束备注**（确认即落 `end_note`）。实现：`store.start` 返回 `string | null`（session_id）；`web/src/lib/conch-mark.ts` 存设备本地一次性标记 `{sessionId, note}`；`handleStop` 单一出口 `consumeConchStartMark` 匹配则预填、无论如何都清除（「继续这段」再结束不重复预填）。端到端验证：预填精确匹配、二次结束为空。

## 2026-08-24 动效与交互升级（补记）

针对"整体偏素、提醒太轻柔"：联网调研（番茄钟/游戏感 juice/纯 CSS 动效技法）后落地，设计与验收见 `docs/动效与交互升级-设计-2026-08-24.md`。

- **提醒升级（核心）**：级别上升瞬间一次性「敲击 + 全视口内缘闪光」（此前只有静态切换，无事件感）；L2/L3 新增「回到专注」召唤胶囊——暂停态点击即继续、空闲态点击即以当前科目开新段；结束卡不显示（避免与备注确认动作竞争）。声音梯度保留既有（L2/L3 各一次）。
- **状态转换 fx**：开始点火（内缘光环 0.7s）、暂停帷幕（0.5s 掠过）、继续回升（0.6s）、结束卡改轻弹簧入场（过冲 1.15，采纳审计意见未用 1.4 弹跳）+ 光晕一次性绽放（原呼吸改绽放）。
- **微交互**：选科 pill 弹跳、海螺入口 hover 轻摆。
- 约束守住：不引 GSAP（调研只取技法）、无阻断弹窗（2026-08-21 决策）、循环 ≥2.4s、reduced-motion/关动画全降级（edge-flash/敲击直接隐藏，CTA 停止浮沉）。
- 验证：四态转换逐帧截图核验（点火/帷幕/回升/结束卡）+ level0 无 CTA；单测与 e2e 回归。

## 2026-08-24 复刻三项：脉冲环 / 休息进度环 / 数字滚动（补记）

按「复刻优先、二次开发」原则，从头部项目源码复刻三项（出处与实测数据见 `docs/调研笔记-同类项目与动效库-2026-08-24.md`）：
- **CTA 脉冲环**：Magic UI pulsating-button 的 ripple 变体（box-shadow 0→8px 扩散、expo-out 曲线），周期放慢到 2.4s；
- **休息进度环**：Pomotroid TimerDial 的 SVG dasharray/dashoffset 结构，嵌入三处 away-line 胶囊，把「已休息」变成可见额度消耗，颜色随级别（中性→琥珀→红）；
- **结束卡数字滚动**：Magic UI number-ticker 的 useMotionValue+useSpring（damping 60/stiffness 100），中文时长格式化为二次开发。
明确不做：confetti（需 canvas 库）、快速脉冲、新依赖。

## 2026-08-25 跨日自动跟随加固 + Pad 副屏自动全屏（补记）

- **跨日自动跟随**：机制原已存在（Timeline 监听 `store.todayDate` 变化，停留在"旧的今天"则跟随到新今天）；本轮加固——跨日时清掉引用旧日期会话的 popover/hoverPreview，避免陈旧详情残留。
- **Pad 副屏自动全屏**（`App.tsx`）：浏览器强制要求用户手势，故副屏（`detectDeviceRole()`）在**首次触摸/按键**时一次性 `requestFullscreen()`；iPadOS Safari 已支持任意元素全屏（Safari 26+），不支持的环境静默降级；成功进入后用户手动退出 → 本页生命周期内不再打扰。实测：副屏点击后 `fullscreenElement=true`，主控不触发，零报错。
- 设计钩子登记的刻意值：结束卡入场 `cubic-bezier(0.3,1.15,0.5,1)` 轻微过冲（游戏感需求，已从 1.4 调低）。

## 2026-08-25 海螺备选开工/换一换 + 新增元素设计统一（补记）

**海螺备选升级**：`alternative: string|null` → `alternatives: string[]`（1–3 条，prompt 第 7 条要求与主推荐不同方向；解析端去重/截断/兼容旧字段，测试覆盖）。卡片抽出 `ConchCard` 子组件：备选以 32px 静默胶囊呈现，点击即按该备选开工（同样落海螺标记、结束预填）；≥2 条时出现「换一换」轮换（本地轮换不重新请求、零 token）。

**新增元素设计统一摸排**（impeccable critique 流程，一次截图审计全状态）：
- 修正：海螺主推荐文案 17px→22px（`--fs-2xl`，与科目名 15px 拉开层级步进）；回归召唤 CTA 40px→44px（归入主操作档）。
- 核验：休息进度环弧度与「已休息」文案逐秒一致（多状态采样证实）；空闲/运行/暂停/结束卡/设置/海螺浮层六态截图无离群。
- 刻意保留：结束卡入场 `cubic-bezier(0.3,1.15,0.5,1)` 轻过冲（游戏感需求）。

## 2026-08-25 用户标注三问题修复 + 全页面巡检（补记）

用户截图标注（L3 逾期态）三处，修复并全状态双主题巡检：
1. **大计时不居中**：`.timer-seg` 在 5ch 盒内 `text-align:left`，冒号窄于 ch 导致视觉左偏 → 改 `center`。
2. **超时胶囊与「回到专注」不对齐/不协调**：`.away-slot` 改纵向堆叠（column + gap 8px），胶囊在上、CTA 在下统一居中；CTA 去掉多余 margin-top。
3. **控件不跟随氛围/主题色**：三态洗色下不透明表面（分段控件轨/选中块、icon-btn hover/selected、text-btn hover、seg-control）改 `color-mix(...% , transparent)` 半透明，洗色透出即自动跟随；亮/暗主题分别调档。
巡检覆盖：L3 亮/暗、空闲亮/暗、运行、结束卡、海螺浮层、近 7 天（前轮）——无其他离群。

## 2026-08-25 跨端结束备注 + 移除「回到专注」绿色按钮（补记）

- **跨端结束备注**：任一端结束会话未填 `end_note` 时，其他端在 5 分钟窗口内经轮询呈现同一张结束卡可补填（确认后落库、各端卡片消失）；`/sessions` 增补 `end_note` 字段供判断（原 `note` 是 end??intent 合并值，区分不了）；seen 集合保证本端已关闭的不重复弹。e2e 新增跨设备补填用例。
- **移除「回到专注」绿色 CTA**（用户决策：与开始/继续按钮语义重复）：returnCta JSX、.return-cta 及 cta-call/cta-ripple 关键帧全删；逾期召回仍由洗色 + 敲击 + 升级音 + 既有开始/继续入口承担。
- **本端关闭不再弹**：结束卡确认关闭后写 localStorage 标记 `[sessionId, 本端stop时刻]`（±10s 容差匹配服务端 ended_at，因 RTT 偏差），刷新/重开不反复弹；「继续这段」恢复不写标记（再结束是新 ended_at，仍会提醒）。e2e 隔离在关闭水合卡时填备注，从服务端断污染。

## 2026-08-25 联想等安卓平板识别修复（补记）

用户联想平板未触发副屏/自动全屏。根因：Chrome 在安卓**平板**上的 UA 也带 "Mobile"，旧启发式「Android 且不含 Mobile」失效。新识别：显式平板型号段（Tablet/Tab/MediaPad/MatePad/Pad/SM-T/SM-X/TB-/YT-/Redmi Pad/Mi Pad；不裸匹配 Lenovo 防误伤联想手机）→ 兜底粗指针+短边≥600 CSS px（手机短边普遍<450）。全屏手势监听补 touchend/click（安卓触摸更可靠）。验证：TB-J606F UA→副屏、Pixel 7 UA→主控。

## 2026-08-25 全局 UI/UX 审计与收敛（补记）

用户要求把近期叠加的页面、交互、设计做全局审计；以 `docs/DESIGN.md` 为现有视觉事实来源，结合实际源码阅读与浏览器几何核验，按“修真实行为 → 减少同权控件 → 宽窄屏重排 → 一次回归”完成：

- **跨端结束备注收卡（P1 已修）**：结束卡水合后原 effect 因 `lastStopped` 直接 return，不会检查另一端写入的 `end_note`；空闲态又只 30s 轮询。结束卡展示期间临时 2s 刷新，最新 `sessions` 出现 `end_note` / `voided` 即收卡回主页；只读监督端不展示可编辑结束卡。跨设备 e2e 断言另一端补备注后本端 ≤5s 收卡。
- **时间轴可扫描性**：工具栏由一串平铺按钮改为可识别的视图 / 日期浏览 / 回顾与建议三组；移动端收窄组间距，去掉 12px 的隐性横滑。移动端会话详情 popover 曾按被撑宽时间轴容器定位、真实越出 390px 视口，现固定在 12px 安全边距内；此前长期失败的视觉用例随之通过。
- **7 天报告与设置层级**：宽屏报告在有数据时为“汇总指标 + 科目分布”双栏、泳道单行全宽；空数据不留空侧栏。设置的外观/结束提示音/动画改标签-控件同行，全宽项（环境音、全屏、说明）保留纵向；退出登录后立即关闭设置层进入只读态。
- **动效与无障碍**：去除结束卡过冲，收敛到既有 expo；reduced-motion/应用内关动画不再用 0.01ms 闪回起始帧，改为停动画/过渡、保留静态 L2/L3 状态。
- **GitHub 源码借鉴**：实际阅读 Pomotroid 的 Timer/TimerDial/TimerFooter（数字绝对居中于表盘层、控制主次分级、固定小控件尺寸）与 Super Productivity focus-mode / Pomodoro 设置的分离对话框架构；取“状态层、控件分组、宽窄屏信息重排”原则，未照搬其固定番茄业务。
- **确定性检查**：Impeccable `detect.mjs --json web` 返回 `[]`；隔离浏览器测量 1280px 报告无横向溢出（指标 656px、科目 230px、泳道 910px）；完整 e2e 预计作为本次最终门禁记录。

## 2026-08-25 海螺语义缓存（补记）

用户明确缓存语义：只要 LLM 决策变量——**已完成时间线及完成备注**——没有变化，推荐就应持续有效；开始/暂停/继续不应消耗新 token。实现：

- migration `0004_conch_revision.sql` 建 `conch_timeline_state` 单行 metadata；`conch_revision` 只在完成计入专注、改完成备注、retime、adjust-start、void、重开已结束会话时推进；短误触 stop 不推进。
- `GET /api/v1/conch/revision`（owner-only）仅读这一行；打开已有缓存时用它校验，不拉整条时间轴、不调模型。相同 revision 长期复用，跨端完成/备注后立刻失效；`POST /conch/ask` 和 `/state` 都带 `conch_revision`。
- 海螺 context 收敛为已 stopped 且计入的事实：进行中/暂停会话及当前秒数完全排除，避免“当前进行几秒”改变模型输入；卡片的“进行中”徽标仍由前端实时 active session 覆盖。
- 前端 state 排序修复：同一服务端毫秒但 revision 更高的写后快照不再被当成旧响应丢掉；stop/switch/note 等语义变化等待权威 refresh 落地。E2E 覆盖：开始→暂停→继续保持 askCount=1，完成后 askCount=2。

## 2026-08-26 跨端结束指标一致性 + 左端刻度清理（补记）

- **平板结束卡不及时收回**：水合卡出现后 effect 对 `lastStopped` 直接 return，另一端填完 end_note 时永不检查最新 sessions；修为结束卡展示期 2s 刷新，发现 `end_note` / `voided` 立即收卡。只读端不水合可编辑卡。
- **两端“已记录本次投入”时间不同**：发起端原用本机单调秒数，平板端原用当天裁剪 `active_seconds`。服务端 stop 与 `/sessions` 现返回会话全量 `session_active_seconds`、`longest_continuous_seconds`、`last_continuous_seconds`、`last_continuous_ended_at`；两端结束卡均以同一服务端事实渲染。
- **结束卡指标**：正常会话显示「本次总专注 / 最长连续专注」，今日累计退为弱上下文。研究取舍：PMID 36859717 支持系统性休息的情绪/效率价值，指标用于观察长时训练，不把 3 小时不间断设为目标。E2E 以暂停→继续的双段会话断言两端指标文本一致。
- **时间轴左端**：首刻度保留线但不渲染具体时间文字，防与轨道/静默区边界标签重合；末刻度仍显示。E2E 固化此契约。

## 2026-08-26 Cloudflare 75% 请求告警审计与收敛（补记）

收到 Workers/Pages Functions 日请求 75% 告警。根因不是单点异常：旧活动页每 3s 并发 `/state` + `/sessions` = 57,600 Worker 请求/日，偏好 15s 再 5,760，单活动客户端约 63,360；`run_worker_first:true` 还让静态 HTML/JS/CSS/字体进入 Worker。完整公式、官方链接、控制台待办见 `docs/Cloudflare-配额审计-2026-08-26.md`。

已实施：
- `run_worker_first` 改仅 `/api/*`，static assets asset-first；`not_found_handling: single-page-application` 保持 SPA fallback；`web/public/_headers` 接管静态 CSP/XFO/nosniff/HSTS 和 hash assets immutable cache。
- 新 `/api/v1/snapshot`：一次 Worker/D1 读取返回同次 state + 当日 sessions；SPA 内部从双请求改单请求，外部 `/state`/`/sessions` 契约保留。
- 可见活动页 10s、空闲 120s，隐藏页 0；恢复可见即时校验；偏好 5min 且隐藏页停。结束卡跨端协作请求分段退避（2s→10s→30s）。
- E2E 49/49，Wrangler dry-run 接受新 static-asset routing。部署后应在 Analytics 复核 `/api/v1/snapshot` 成为主动态路径、静态资源不再计 Workers invocation。

## 2026-08-27 面向科目消费者的公开范围 API（补记）

消费者读取历史受限：本轮尝试按用户授权定向读取数学/408/英语/政治/总节奏任务近期回合，但云端会话存储接口返回 unavailable/not found；因此只按用户明确列出的消费场景实现，不臆造任务历史。

新增/扩展契约：

- `GET /api/v1/sessions?date=YYYY-MM-DD` 保持兼容；新增 `from/to`（含首尾、最多 31 北京日）、`subject_id`、`aggregate_group`（`408` 四模块）、`status`、`has_note`。`date` 与范围互斥，科目与聚合互斥。范围只读一次 sessions/segments，再在内存切片；跨午夜同一 session 只返回一次，`window_active_seconds` 与 `session_active_seconds` 明确区分。
- 会话公开条目显式含 `intent_note` / `end_note` / 兼容 `note`、窗口裁剪秒数、会话全量秒数、最长/最近连续段及裁剪 segments；默认排除 voided，但 `adjustments_or_revocations` 返回关联会话的更正/撤回摘要，即使更正发生在更晚日期也可定位。
- `GET /api/v1/daily-summaries?from=&to=&timezone=Asia/Shanghai`：最多 31 日，一次 D1 范围读 + 领域层逐日切片；空日真实 0，返回范围总计、按科、aggregate、active_dates、每日 session_count，不重复完整 sessions。
- 范围接口 ETag 由范围/过滤/revision 确定；运行中会话与查询范围相交时禁用 304。动态会话详情为 `private, no-cache, must-revalidate`；API 路径由 Worker-first，SPA fallback 不吞 API 400/404。

回归证据：旧单日、7/31 边界、非法组合、数学/408/备注过滤、跨午夜去重、paused/running segments、void/retime/adjust-start/note ETag、运行中 304 禁止、SQLite/D1 query parity、Worker routing contract；server 47 测试中覆盖。

科目任务调用速记（全部公开只读、不得传 Cookie/PIN/token）：

```bash
BASE='https://clock.4c666.top'
TODAY=$(TZ=Asia/Shanghai date +%F)
FROM7=$(TZ=Asia/Shanghai date -v-6d +%F 2>/dev/null || date -d '6 days ago' +%F)
FROM31=$(TZ=Asia/Shanghai date -v-30d +%F 2>/dev/null || date -d '30 days ago' +%F)

# 数学当日 / 408 当日
curl -s "$BASE/api/v1/sessions?date=$TODAY&subject_id=math"
curl -s "$BASE/api/v1/sessions?date=$TODAY&aggregate_group=408"
# 数学/408 近7日、总节奏近31日、历史批阅备注定位
curl -s "$BASE/api/v1/sessions?from=$FROM7&to=$TODAY&subject_id=math"
curl -s "$BASE/api/v1/sessions?from=$FROM7&to=$TODAY&aggregate_group=408"
curl -s "$BASE/api/v1/daily-summaries?from=$FROM31&to=$TODAY&timezone=Asia%2FShanghai"
curl -s "$BASE/api/v1/sessions?from=$FROM31&to=$TODAY&status=stopped&has_note=true"
```

## 2026-08-25 全局体验审计收敛 + 跨端结束卡及时收回（补记）

用户要求全系统审计后，按“先修真实行为、再减杂、最后核验”的有界流程落地：

- **跨端结束卡及时收回（P1）**：旧水合 effect 在 `lastStopped` 存在时直接 return，永不检查另一端是否已写入 `end_note`；且空闲轮询 30s。修复为结束卡展示期间临时 2s 对表，最新会话为 `end_note` / `voided` 时立即清 `lastStopped` 回主页；只读监督端不水合可编辑卡。跨设备 e2e 断言端 B 补备注后端 A ≤5s 收卡。
- **时间轴工具栏**：由一排平铺控件改成“视图 / 日期浏览 / 回顾与建议”三个 `role=group`，组内 8px、组间细分隔，降低同权按钮造成的扫描负担。
- **近 7 天报告**：有科目数据的宽屏报告改“汇总指标 + 科目分布”双栏，泳道独占下一行；≤760px 自然单栏。无科目分布时保留全宽指标，避免空白侧栏。隔离数据实测 1280px：无横向溢出，指标 656px / 科目 230px / 泳道 910px。
- **设置**：外观、结束提示音、动画改标签-控件同行，环境音/全屏/说明保留全宽；全屏行文案由 query 参数诊断串收敛为当前设备角色。
- **动效无障碍**：`prefers-reduced-motion` 与应用内关闭动画由 0.01ms 归零改为禁用动画/过渡并保留静态终态，避免起始帧闪回。
- **源码借鉴**：复读 Pomotroid（Timer 外壳将表盘、数字、控制拆层；控制按主/次分级）和 Super Productivity（设置使用专门对话框/结构化 focus-mode 子页）的实际源码；本项目取其“分组控制、主次层级、宽屏双栏而窄屏单栏”原则，不照搬固定番茄钟业务。
